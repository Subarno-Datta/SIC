zero vector of size 5, one vector of size 5, 3x3 identity matrix, 3x3 diagonal matrix, symmetric matrix

prices of products: 200, 150, 100... quantity: 2, 5, 3

from sklearn.datasets import load_digits
X = load_digits().data

//find similarity between 2nd and 15th image using dot product, write the numpy code
//how can you compute similarity between all images efficiently?

find inverse of the matrix [[2, 1],[5, 3]]

solve the system using matrix form:
2x + y = 5, x + 3y = 6
use numpy to compute solution

Section D: Linear Regression concept (Boston-like problem)

given dataset with features: pollution(POLL), rooms(RM), Distance(DIS), Age(AGE)
Matrix: A = [[1,2,3,4],[2,1,0,3],[3,2,1,5],[4,3,2,1]] ... b = [10, 8, 12, 15]

1. Find weight vector using Linear equation (Ax = b)
2. Interpret the result: a. Which features are directly proportional?   b. Which features are inversely proportional?


Q8. Solve the above problem using least squares method

Section E: Distance Measures

Q9. a = [5, 3, 0, 1], b = [4,0,0,1], c = [1,1,0,5] ... compute euclidean distance... identify closest and farthest users

Q10. Using same data, compute cosine similarity, identify closest and farthest users


Section F: Eigenvalues

Q11. Find Eigenvalues of A = [[4, 2],[1, 3]]


Section G: Logistic Function

Q12. Find inverse of logistic function: y = 1/(1 + exp(-x))


Section H: Differentiation

Q13. Differentiate:
1. f(x) = x^3 + 2x
2. f(x) = log(3x+1)
3. f(x) = e^(2x)

Section I: Partial Derivatives

Q14. Find first order partial derivatives: f(x,y) = e^(xy)

Section J: Sympy Based Questions

Q15. Using Sympy, find derivative of f(x)= x^3 + 3x, f(x) = log(x^3 + 1), f(x) = e^(3x)

Q16. Using Sympy, compute partial derivatives.. f(x,y) = e^(x+y)

Q17. Create a movie recommendation system: use cosine similarity, input: user ratings matrix, output: most similar users


Q18. Explain:
1. Difference between euclidean distance and cosine similarity
2. when to use each in ML applications

